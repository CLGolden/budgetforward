/**
  * The file that controls what to do with user input
  **/

/**
  * The main controller object
  **/
let controller = {};

/**
  * Initiates the controller
  **/
controller.init = function () {
	
};

/**
  * Listens for changes to the form
  **/

/**
  * Get input from the form. Prepares it to be included
  * in the budgetItems JSON object for local Storage
  **/
controller.getFormInput = function( param ) {
	
};